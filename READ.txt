Aqui segue um refatoramento do Auth.php
Eu todas as funções que eu preciso instanciar o dao passando o pdo para o parametro dele 
eu crio uma variável na classe Auth e substituo o 

  $userDao = new UserDaoMysql($this->pdo);
  $user = $userDao->findByToken($token);

  por 

   $user =  $this->dao->findByToken($token);
   